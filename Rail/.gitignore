# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node.js dependencies and build output (not needed in Git)
node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Ensure package-lock.json is committed (helps with dependency consistency)
# If you're using npm, make sure this file is **not** in .gitignore
# (if it's ignored, you might have issues with builds or deployments)
!package-lock.json

# Ensure your `package.json` is not ignored
# This is critical for Netlify and the build process to work
!package.json
